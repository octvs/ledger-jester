[project]
name = "ledger-jester"
authors = [{ name = "O. C. Taskin", email = "octvs@posteo.de" }]
version = "0.0.1"
dependencies = ["ledger"]
description = "A stripped down fork of ledger-autosync"

[build-system]
requires = ["setuptools"]
build-backend = "setuptools.build_meta"

[project.scripts]
ledger-injest = "ledger_jester.cli:run"
